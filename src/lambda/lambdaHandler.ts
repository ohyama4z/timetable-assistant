// import timetable from './timetable'

export const tametableAssistantHandler = () => {
  // const date = new Date()
  // const day = `${['日', '月', '火', '水', '木', '金', '土'][date.getDay()]}曜日`
  // if (day === '日曜日' || day === '土曜日') {
  //   return timetable['月曜日']
  // }

  // return { statusCode: 200, body: JSON.stringify(timetable[day]) }
  return { aho: 'aho' }
}
